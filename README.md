# Speecher

**Транскрибация голоса по горячей клавише.** Windows 10/11.

[![Последняя версия](https://img.shields.io/github/v/release/itsphererepair1-design/speecher-releases?label=%D0%BF%D0%BE%D1%81%D0%BB%D0%B5%D0%B4%D0%BD%D1%8F%D1%8F%20%D0%B2%D0%B5%D1%80%D1%81%D0%B8%D1%8F&color=2f81f7)](https://github.com/itsphererepair1-design/speecher-releases/releases/latest)

## ⬇️ Скачать

### **[Скачать последнюю версию Speecher](https://github.com/itsphererepair1-design/speecher-releases/releases/latest)**

Открой ссылку и нажми на файл `SpeecherSetup-X.Y.Z.exe` в разделе **Assets** —
скачается установщик, примерно 95 МБ. Эта ссылка всегда ведёт на самую свежую
версию, старые остаются доступны на [странице всех релизов](https://github.com/itsphererepair1-design/speecher-releases/releases).

## Что это

Держишь `Ctrl+Space` в любом окне и говоришь — на экране бело-синий эквалайзер
с живой волной голоса. Отпускаешь — текст расшифрован и уже в буфере обмена.
Вставляешь через `Ctrl+V` когда и куда нужно.

Распознавание — Groq API (Whisper large-v3, бесплатный tier), офлайн-запас —
faster-whisper прямо на твоём компьютере, без интернета.

## Установка

1. Скачай `SpeecherSetup-X.Y.Z.exe` по ссылке выше.
2. Запусти. Права администратора не нужны.
3. Установщик спросит, качать ли модели для офлайн-распознавания.

Windows SmartScreen может показать синее окно «Windows защитила ваш компьютер» —
это обычное дело для программ без платной подписи. Нажми **Подробнее** →
**Выполнить в любом случае**.

## Проверка скачанного файла

К каждому релизу приложен `SHA256SUMS.txt` — контрольная сумма установщика.
Сверить перед запуском можно так: открой PowerShell в папке со скачанным
файлом и выполни

```powershell
Get-FileHash .\SpeecherSetup-1.2.1.exe -Algorithm SHA256
```

Строка в ответе должна совпасть с той, что лежит в `SHA256SUMS.txt` рядом
с установщиком. Совпала — файл дошёл ровно таким, каким я его собрал.

## Приватность

Программа слушает микрофон и умеет отправлять голос в облако, поэтому
рассказываю честно и по пунктам, что происходит с данными.

**Запись идёт только пока зажата горячая клавиша.** Отпустил — запись
остановилась. Фонового прослушивания нет.

**Звук живёт секунды.** Запись сохраняется во временный WAV-файл, уходит
на распознавание и удаляется сразу после — в том числе если распознавание
упало с ошибкой. Никакого архива записей на диске не остаётся.

**Куда уходит звук.** При облачном распознавании WAV отправляется в Groq
(`api.groq.com`) — и больше никуда. Если включено локальное распознавание
и локальная доводка текста, звук не покидает компьютер вообще. Когда часть
обработки облачная, программа говорит об этом прямо в настройках и спрашивает
согласие — «локальным» режим называется только тогда, когда локальны оба
звена: и распознавание, и доводка текста.

**Что программа хранит у себя.** История расшифровок, словарь, настройки
и ключи лежат в `%LOCALAPPDATA%\Speecher` на твоём компьютере. Ни строчки
из этого никуда не отправляется. История — обычная база SQLite, её можно
чистить прямо из окна программы.

**Ключи API** хранятся зашифрованными средствами Windows (DPAPI): расшифровать
их может только твой пользователь на твоей машине. В открытом виде на диск
они не пишутся и в логи не попадают.

**Журнал вызовов API** (вкладка «Логи и статистика») хранит только служебные
цифры: провайдер, модель, время ответа, токены и оценку стоимости.
Текстов расшифровок в нём нет.

**Никакой телеметрии.** Программа не собирает статистику использования, не
отправляет отчёты о падениях и не считает установки. Кроме Groq она сама
обращается ровно в два места: `huggingface.co` — скачать модель для офлайна,
если ты её попросил, и `api.github.com` — проверить, не вышла ли новая версия.
Запрос за обновлением уходит без единого идентификатора: только имя и номер
версии программы. Ещё есть `openrouter.ai`, но только если ты сам укажешь его
в настройках как свой endpoint.

## Обновление

Программа сама проверяет новые версии и предлагает обновиться. Если ставишь
руками — просто запусти новый установщик поверх, удалять ничего не нужно.
История расшифровок, словарь, ключи и настройки лежат отдельно от программы
и переустановку переживают.

## Что нового

Список изменений каждой версии — на [странице релизов](https://github.com/itsphererepair1-design/speecher-releases/releases).

## Нашёл проблему с безопасностью?

Не открывай публичный Issue — напиши мне лично через вкладку **Security** →
**Report a vulnerability**. Подробности в [SECURITY.md](SECURITY.md).

## Условия использования

Speecher — закрытая программа. Исходный код не публикуется, здесь
раздаются только готовые сборки. Все права на программу принадлежат автору.

Можно: пользоваться программой и ставить её на свои компьютеры.

Нельзя: перепродавать, распространять изменённые сборки, вскрывать
и разбирать программу.

Программа поставляется «как есть», без гарантий. Автор не отвечает за
возможные убытки от её использования. Распознавание речи выполняется
сторонним сервисом Groq на его условиях — за качество и доступность
чужого сервиса я отвечать не могу.
